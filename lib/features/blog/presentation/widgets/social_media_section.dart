import 'package:flutter/material.dart';

class SocailMediaShare extends StatelessWidget {
  const SocailMediaShare({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
